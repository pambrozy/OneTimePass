import SwiftUI
import OneTimePass

struct TOTPView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct TOTPView_Previews: PreviewProvider {
    static var previews: some View {
        TOTPView()
    }
}
